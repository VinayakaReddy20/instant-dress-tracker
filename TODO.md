# TODO: Fix Customer Delete Account Functionality

## Tasks
- [x] Modify handleDeleteAccount function in src/pages/CustomerProfile.tsx to remove the auth user deletion attempt (using supabase.auth.admin.deleteUser), keeping only the customer record deletion and sign out.
